package main
import "fmt"
var max,min,i int
var n int =6
var arr[6] int
func maxmin(arr,n int) (int,int){
  mid=n/2
  maxmin(arr,mids)
  for i<n{
    if min>arr[i]
      min=arr[i]
    if max<arr[i]
      max=arr[i]
    i+=1
  }
  return max,min
}
func main(){
  maxmin(12,3,1,16,5,21,13,24,n)
  fmt.Print(max," ",min)
}
